namespace GradeBook {
    public class Statistics {
        public Statistics(Book book) {
            // high = book.HighGrade();
            // low = book.LowGrade();
            // average = book.Average();
            // sum = book.Sum();
            // count = book.Count();
        }
        public double high;
        public double low;
        public double average;
        public double sum;
        public int count;
    }
}

